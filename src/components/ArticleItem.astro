---
interface Props {
  title: string;
  image?: string;
  createdAt: string;
  url: string | undefined;
  tags: string[];
}

const {title, createdAt, url, tags} = Astro.props;
const formattedDate = createdAt.slice(0, 10);
---

<article
  class="group bg-header rounded-lg p-6 transition-all h-full flex flex-col border-t-2 border-accent hover:shadow-lg hover:shadow-accent/20"
>
  <div class="mb-3">
    <span class="text-gray-400 text-sm">{formattedDate}</span>
  </div>
  <a href={url} class="block group grow">
    <h2 class="text-xl font-bold text-white group-hover:text-accent transition-colors mb-4">
      {title}
    </h2>
  </a>
  <div class="flex flex-wrap gap-2 mt-auto">
    {
      tags.map((tag) => (
        <a href={`/tags/${tag}`} class="inline-block">
          <span class="inline-block px-3 py-1 text-sm text-white bg-[#21262d] hover:bg-[#2d333b] rounded-full transition-colors">
            #{tag}
          </span>
        </a>
      ))
    }
  </div>
</article>
